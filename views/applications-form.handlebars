          <div>
              <h2><b>Apps</b></h2>
              </div>
          <hr/>
          <div class="container">
                <div class="row">
                    <div class="col-lg-12 col-md-12 col-sm-12" class="row justify-content-center">
                        <div class="card text-left" style="width: 100%;">
                            <div class="card-body">
                                <h2 >Create your first app</h2>
                                <p class="card-text ">
                                    <label class="form-control-danger control-label">{{errors}}</label>
                                   <form method="POST" action="/auth/applications/create">
                                        <div class="form-group">
                                            <input type="text" class="form-control" name="appName" placeholder="App Name">
                                        </div>
                                        <div class="form-group">
                                            <input type="url" class="form-control" name="appUrl" placeholder="App URL (ex: https://mydomain.com)">
                                        </div>
                                        <div class="form-group">
                                            <input type="text" class="form-control" name="allowedCallbakUrl" placeholder="Allowed Callback URL's">
                                            <small id="emailHelp" class="form-text text-muted">After the user activates pods or data sources, we will only call back to any of these URLs. You can specify multiple valid URLs by comma-separating them (typically to handle different environments like QA or testing). Make sure to specify the protocol (https://) otherwise the callback may fail in some cases.</small>
                                        </div>
                                        <div class="form-group">
                                            <input type="text" class="form-control" name="companyUri" placeholder="Company URI">
                                        </div>
                                        <button type="submit" class="btn btn-primary"><i class="material-icons" style="font-size:28px">apps</i>&nbsp;&nbsp;&nbsp;Create app</button>
                                        </form>
                                    </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
